现在我棋盘格给你了，代码相关修改也给你了，我需要你根据我的要求帮我重新写下有关代码分门别类放入“手眼标定相关”文件夹中：

## 1.maixcam相关的：

还是要包括main文件和一些功能py文件：`识别二维码（01）`、`识别物料（红绿蓝，02）`、`识别色环（06）`，其余没用的原来MaixCam涉及到的没必放进去。

因为我每次任务，工位需要对应不同高度H，所以我想你还可以再进行略微修改放入文件夹中，要求：

1. 代码尽量不修改，如要修改进行小幅度修改即可
2. 识别二维码代码可以不修改，后面设计到高度问题，所以我需要你让我可以自由修改高度的变量以进行调试
3. 识别物料分为**三种**：我需要分别定高度，所以你要保证我在每种情况下也能让我串口修改模式。

- 第一种：原料区（高度为转盘高度90 + 物块高度70）；
- 第二种：粗加工区（放在地面上，故仅包括物块高度70）；
- 第三种：暂存区一层（需要放到第一层物料上方进行码垛，故仅包括第一次物块高度70）

4. 识别色环的代码我感觉不需要进行修改，因为我们的色环是在地面的，所以高度是固定的。
5. 记得要用cx,cy来表示图像坐标系中的中心点位置，方便后续的手眼标定计算。（应该不需要dx,dy吧）

## 2.ROS相关：

你先看看老师给的代码我有哪些可以用的放进来，然后修改代码要保证我可以和maixcam进行通讯。
_（原本 `xrobot2_ws`里的 `xrobot_cv`下的东西已可以支撑我的通讯，你先以此为依照进行修改，我后面可能会给你新的文件再次修改。）_

然后可以的话，我希望你可以简化我的发送串口的代码，使它更简洁，更方便我操作。

## 3.STM32相关：

你看原本老师给的那几个文件夹里的我可以怎么用，这里你就别单独新建了，直接在firmware文件夹下修改相关文件即可，看能不能完成我的需求。
比如：根据舵机旋转角度得到正运动学结果，我们大致机械臂的模样你已经知道了，就是scara型机械臂在 `底部旋转关节`（假设z轴朝上）上面加了一个 `沿z轴的平移自由度`,然后还有一个 `旋转自由度`（z轴依旧朝上），最后还有一个 `末端执行器`（夹爪）。所以根据这个结构，我们需要根据舵机旋转角度得到机械臂末端的位置和姿态，这样我们才能进行手眼标定。

## 4.OpenCV处理代码

和你前面说的手眼标定的操作有关的代码，我觉得你可以把它放在一个单独的文件夹里，你也可以看看我clone的那个里面有没有能用的代码借鉴一下

## 5.README文件

README文件中需要包括以下内容：

1. jeston nano和Maixcam通讯方式，彼此要传输什么数据，jetson nano要怎么样控制Maixcam的模式
2. 手眼标定任务完成具体流程，包括每个步骤需要执行的代码文件和操作说明
3. 回顾之前对话，完成手眼标定任务原理等分析与讲解，要有必要的公式，图片你也可以去网上找，来辅助讲解

## 6. 其他

- 代码中可能需要添加一些中文注释，说明每个函数的作用和参数的含义。
- 代码中可能需要添加一些日志，方便调试。
- 代码中可能需要添加一些异常处理，防止程序崩溃。

以上只是我大致想到的修改建议，你可以根据实际情况进行调整修改。
