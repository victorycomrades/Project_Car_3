<?xml version="1.0"?>
<package format="2">
  <name>maixcam_uart_bridge</name>
  <version>0.1.0</version>
  <description>ROS1 bridge node for MaixCam UART serial communication. Receives vision results (QR/BLOB/RING) from MaixCam via serial port, publishes as ROS topics, and forwards MODE commands back to MaixCam.</description>
  <maintainer email="user@example.com">user</maintainer>
  <license>MIT</license>

  <buildtool_depend>catkin</buildtool_depend>

  <depend>rospy</depend>
  <depend>std_msgs</depend>
  <depend>geometry_msgs</depend>
  <exec_depend>python3-serial</exec_depend>

  <export>
    <build_type>catkin</build_type>
  </export>
</package>
