# 手眼标定

本文档是本项目的实际操作流程。后续标定时按顺序执行，不要跳过“固定相机”和“记录末端位姿”这两步。

## 1. 总体目标

本项目相机固定在机械臂末端，属于眼在手上。最终要得到两个结果：

1. 相机内参：`camera_intrinsics.yaml`，包含相机矩阵 `K` 和畸变参数 `dist_coeffs`。
2. 手眼外参：`T_gripper_camera.yaml`，包含相机坐标系到末端/夹爪坐标系的固定变换 `T_gripper_camera`。

抓取时使用：

```text
P_base = T_base_gripper * T_gripper_camera * P_camera
```

其中：

- `P_camera`：物块中心点在相机坐标系下的位置。
- `T_gripper_camera`：手眼标定得到的外参。
- `T_base_gripper`：机械臂当前末端位姿，由 firmware 根据舵机角度和正运动学计算。
- `P_base`：物块中心点在机械臂基坐标系下的位置，后续用于抓取。

## 2. 棋盘格准备

使用项目中的文件：

```text
chessboard_27mm/棋盘格27_27.pdf
```

打印要求：

1. 打印时关闭“适应页面”“缩放到纸张大小”等选项，必须按 100% 原始尺寸打印。
2. 打印后用尺子测量单个黑白方格的边长。
3. 如果实测边长不是 27mm，后续命令里的 `--square-mm` 必须改成实测值。
4. 棋盘格要尽量贴在硬纸板或平整板材上，避免弯曲。

OpenCV 参数使用内角点数量，不是方格数量。本棋盘格默认按：

```text
pattern-cols = 9
pattern-rows = 6
square-mm = 27
```

## 3. Jetson 与 MaixCam 通信

MaixCam 只负责识别二维码、物料和色环，不做巡线。

接线：

```text
MaixCam A19 TX -> Jetson RX
MaixCam A18 RX -> Jetson TX
MaixCam GND    -> Jetson GND
```

串口参数：

```text
115200, 8N1
```

MaixCam 运行主脚本：

```text
MaixCam/maixcam_tests/maixcam_uart_vision.py
```

Jetson 启动 ROS bridge：

```bash
cd xrobot4_ws
catkin_make
source devel/setup.bash
roslaunch maixcam_uart_bridge maixcam_uart_bridge.launch port:=/dev/ttyUSB0
```

常用模式命令：

```bash
rostopic pub /vision/mode std_msgs/String "data: 'MODE,QR'" -1
rostopic pub /vision/mode std_msgs/String "data: 'MODE,BLOB,RAW,RED'" -1
rostopic pub /vision/mode std_msgs/String "data: 'MODE,BLOB,PROCESS,ALL'" -1
rostopic pub /vision/mode std_msgs/String "data: 'MODE,BLOB,STORAGE1,BLUE'" -1
rostopic pub /vision/mode std_msgs/String "data: 'MODE,RING,RED'" -1
rostopic pub /vision/mode std_msgs/String "data: 'MODE,IDLE'" -1
```

物料输出格式：

```text
BLOB,color,dx,dy,cx,cy,w,h,area,worksite,height_mm
```

其中 `cx,cy` 是图像坐标，原点是左上角；后续几何定位主要用 `cx,cy`。`dx,dy` 是相对画面中心的偏差，主要用于调试或末端闭环微调。

## 4. 固定眼在手上的结构

手眼标定前必须完成：

1. MaixCam 已经牢固安装在机械臂末端。
2. 相机和夹爪之间不能再松动或改变角度。
3. 标定期间棋盘格放在桌面或固定支架上，不要移动。
4. 机械臂移动采样时，只改变机械臂姿态，不改变棋盘格位置。

如果标定后移动了相机、换了安装孔、重新拧了支架，之前的 `T_gripper_camera` 就不能继续用于精确抓取，需要重新做手眼标定。

## 5. 相机内参标定

### 5.1 采集图片

采集 15 到 30 张棋盘格照片。要求：

- 棋盘格完整出现在画面中。
- 有远近变化，有左右上下位置变化，有一定倾斜角度变化。
- 不要全部都正对相机，也不要全部放在画面中央。
- 图片不能明显模糊。
- MaixCam 标定和后续使用时分辨率要一致，建议固定 `320x240`。

建议目录：

```text
calibration/data/intrinsics/
```

把图片放进去，例如：

```text
calibration/data/intrinsics/img_001.jpg
calibration/data/intrinsics/img_002.jpg
...
```

### 5.2 运行内参标定

在项目根目录执行：

```bash
python calibration/calibrate_intrinsics.py --images calibration/data/intrinsics --pattern-cols 9 --pattern-rows 6 --square-mm 27 --output calibration/output/camera_intrinsics.yaml
```

如果实测棋盘格不是 27mm，把 `--square-mm 27` 改成实测值。

脚本会输出：

```text
calibration/output/camera_intrinsics.yaml
calibration/output/intrinsics_corners/
```

`intrinsics_corners` 中保存了角点检测结果图。检查这些图，确认角点确实贴合棋盘格内角点。

### 5.3 判断内参是否可用

重点看终端输出的平均重投影误差：

```text
Mean reprojection error
```

误差越小越好。如果误差明显很大，通常原因是：

- 棋盘格方格尺寸填错。
- 内角点数量填错。
- 图片太模糊。
- 棋盘格不平整。
- 棋盘格只在画面中央出现，姿态变化太少。

## 6. firmware 末端位姿

手眼标定每一张图片都要配一组机械臂末端位姿 `T_base_gripper`。

当前 firmware 中增加了 SCARA 正运动学入口：

```text
xrobot_firmware/applications/ros/include/xrobot_arm_kinematics.h
xrobot_firmware/applications/ros/src/xrobot_arm_kinematics.cpp
```

RT-Thread shell 中可用命令：

```text
arm_pose_cmd
```

输出示例：

```text
ARM_JOINT,0.123000,0.456000,-0.100000
ARM_POSE,T_BASE_GRIPPER,1.000000,0.000000,0.000000,120.000000,...
```

注意：代码中的舵机 ID、零位、升降换算和夹爪偏移目前是配置项，需要按实物校准后填写。未校准前只能验证流程，不能用于精确抓取。

需要实测并填写的量：

- 底部旋转舵机 ID。
- 升降轴舵机 ID。
- 末端旋转舵机 ID。
- 各关节零位角。
- 舵机正方向是否与模型一致。
- 升降轴每弧度对应的实际升降毫米数。
- 夹爪中心相对末端坐标系的 x/y/z 偏移。

## 7. 采集手眼样本

每组样本必须同时包含：

1. 一张当前姿态下拍到棋盘格的图片。
2. 同一时刻机械臂末端位姿 `T_base_gripper`。

建议目录：

```text
calibration/data/handeye/
```

操作步骤：

1. 把棋盘格固定在桌面或支架上。
2. 移动机械臂到第 1 个姿态。
3. 等机械臂完全静止。
4. 保存当前相机图片，例如 `sample_001.jpg`。
5. 在 firmware shell 或 ROS 串口输出中记录 `ARM_POSE,T_BASE_GRIPPER,...`。
6. 把 16 个矩阵数字填入 `handeye_samples.json`。
7. 换一个明显不同姿态，重复以上步骤。

至少采 15 组，建议 20 组左右。姿态要有变化：

- 棋盘格出现在画面左、右、上、下不同位置。
- 机械臂末端有不同旋转角度。
- 相机到棋盘格的距离有变化。
- 不要只在同一高度同一角度附近轻微移动。

样本 JSON 格式参考：

```json
{
  "samples": [
    {
      "image": "data/handeye/sample_001.jpg",
      "T_base_gripper": [
        [1, 0, 0, 120],
        [0, 1, 0, 0],
        [0, 0, 1, 180],
        [0, 0, 0, 1]
      ]
    }
  ]
}
```

可以复制：

```text
calibration/handeye_samples.example.json
```

另存为：

```text
calibration/data/handeye/handeye_samples.json
```

## 8. solvePnP 在这里做什么

每张手眼图片中，脚本会先检测棋盘格角点：

- `objectPoints`：棋盘格内角点在棋盘格自身坐标系下的三维点，单位 mm。
- `imagePoints`：同一批角点在图片上的像素坐标。

棋盘格平放时，可以把棋盘格坐标系定义为：

```text
第一个内角点 = (0, 0, 0)
向右每个角点 x 增加 27
向下每个角点 y 增加 27
所有角点 z = 0
```

`solvePnP` 使用 `objectPoints`、`imagePoints`、相机内参和畸变参数，求：

```text
T_camera_target
```

它表示棋盘格坐标系在相机坐标系下的位置和姿态。

## 9. 执行手眼标定

运行：

```bash
python calibration/calibrate_handeye.py --samples calibration/data/handeye/handeye_samples.json --intrinsics calibration/output/camera_intrinsics.yaml --pattern-cols 9 --pattern-rows 6 --square-mm 27 --output calibration/output/T_gripper_camera.yaml
```

输出：

```text
calibration/output/T_gripper_camera.yaml
```

这就是眼在手上的外参。

`calibrateHandEye` 使用的是多组：

```text
T_base_gripper
T_camera_target
```

求出固定不变的：

```text
T_gripper_camera
```

如果脚本提示有效样本太少，说明有些图片没有检测到棋盘格，需要重新拍或剔除。

## 10. 验证标定结果

准备一个当前末端位姿 JSON，例如：

```text
calibration/data/current_pose.json
```

内容：

```json
{
  "T_base_gripper": [
    [1, 0, 0, 120],
    [0, 1, 0, 0],
    [0, 0, 1, 180],
    [0, 0, 0, 1]
  ]
}
```

用 MaixCam 识别物料，得到：

```text
BLOB,RED,dx,dy,cx,cy,w,h,area,RAW,160
```

把 `cx,cy,height_mm` 填入命令：

```bash
python calibration/pixel_to_base.py --intrinsics calibration/output/camera_intrinsics.yaml --handeye calibration/output/T_gripper_camera.yaml --t-base-gripper calibration/data/current_pose.json --cx 154 --cy 102 --height-mm 160
```

输出示例：

```json
{
  "frame": "base",
  "x_mm": 123.4,
  "y_mm": 56.7,
  "z_mm": 160.0
}
```

用尺子或机械臂实际移动验证这个点是否接近真实物块位置。若误差很大，优先检查：

- 内参图片是否和实际使用分辨率一致。
- 棋盘格尺寸是否填对。
- 手眼样本中的图片和 `T_base_gripper` 是否一一对应。
- firmware 正运动学参数是否还是占位值。
- 相机在标定后是否移动过。

## 11. 抓取时怎么用

实际抓取流程：

1. Jetson 让机械臂到观察姿态。
2. Jetson 发 MaixCam 模式命令，例如：

   ```bash
   rostopic pub /vision/mode std_msgs/String "data: 'MODE,BLOB,RAW,RED'" -1
   ```

3. MaixCam 输出物料中心：

   ```text
   BLOB,RED,dx,dy,cx,cy,w,h,area,RAW,160
   ```

4. ROS 读取当前 `T_base_gripper`。
5. 使用 `camera_intrinsics.yaml`、`T_gripper_camera.yaml`、`cx,cy,height_mm` 计算 `P_base`。
6. 机械臂移动到 `P_base` 上方的安全高度。
7. 下降到抓取高度，夹爪闭合。
8. 抬起并进入下一步任务。

实际控制时可以使用两层策略：

- 粗定位：用 `cx,cy,height_mm` 算 `P_base`，直接规划到物块附近。
- 微调：末端接近目标后，用 `dx,dy` 做小范围闭环，让目标靠近预设抓取像素点。

不要把 `dx,dy` 当成最终几何坐标。它只是像素偏差，真正进入基坐标计算的是 `cx,cy` 和目标高度。

## 12. 常见问题

### calibrateCamera 只要图片就行吗

不只要图片。还需要棋盘格内角点数量和每格实际尺寸。脚本会自动从图片中检测 `imagePoints`，但 `objectPoints` 要根据你输入的棋盘格尺寸生成。

### solvePnP 得到的是什么

它得到 `T_camera_target`，也就是棋盘格坐标系相对于相机坐标系的位置和姿态。手眼标定需要每张图的这个结果。

### calibrateHandEye 得到的是外参吗

是。眼在手上时，它得到 `T_gripper_camera`，表示相机坐标系相对于机械臂末端/夹爪坐标系的固定变换。

### 物块不在画面中心怎么办

没有关系。`cx,cy` 本来就是物块中心在图像中的绝对像素坐标，物块可以在画面任意位置。只要内参和外参正确，就可以从这条像素射线和目标高度平面求交得到基坐标。

### 什么时候要重新标定

以下情况需要重新做手眼标定：

- 相机相对夹爪的位置或角度变了。
- 相机固定支架松动后重新固定。
- 更改相机分辨率或镜头。
- firmware 正运动学参数大幅修正后，建议重新采样验证。
